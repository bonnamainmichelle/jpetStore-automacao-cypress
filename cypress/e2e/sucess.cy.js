import Register from "../site/register";


 describe('page', () => {
  
  it.only('register now', () => {
    Register.registerSite();
  });

  it.only('login site', () => {
    Register.loginAddCart();
  })






});